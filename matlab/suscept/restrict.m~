ind = find(field == 150);
% ind = find(thick == thick(length(thick)));
% ind = find(thick == t(12));

freq = freq(ind,:);
chi = chi(ind,:);
thick = thick(ind);
field = field(ind);

% z=z(ind,:);
% s=s(ind,:);
